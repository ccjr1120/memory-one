name: Publish npm package

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write
  id-token: write

concurrency:
  group: publish-main
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Check out source
        uses: actions/checkout@v4
        with:
          ref: main

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          registry-url: https://registry.npmjs.org

      - name: Install dependencies
        run: npm ci

      - name: Bump patch version
        run: npm version patch --no-git-tag-version

      - name: Publish package
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          VERSION="$(node -p "require('./package.json').version")"
          if npm view "@ccjr1120/memory-one@${VERSION}" version >/dev/null 2>&1; then
            echo "@ccjr1120/memory-one@${VERSION} already exists; skipping publish."
          else
            npm publish --access public --provenance
          fi

      - name: Wait for npm registry propagation
        run: |
          VERSION="$(node -p "require('./package.json').version")"
          for attempt in $(seq 1 12); do
            TARBALL="$(npm view "@ccjr1120/memory-one@${VERSION}" dist.tarball 2>/dev/null || true)"
            if [ -n "$TARBALL" ]; then
              echo "@ccjr1120/memory-one@${VERSION} is available: $TARBALL"
              exit 0
            fi
            echo "Waiting for @ccjr1120/memory-one@${VERSION} to propagate (${attempt}/12)..."
            sleep 10
          done
          echo "@ccjr1120/memory-one@${VERSION} did not propagate in time."
          exit 1

      - name: Commit bumped version
        run: |
          VERSION="$(node -p "require('./package.json').version")"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add package.json package-lock.json
          git commit -m "📦️ chore: bump version to ${VERSION} [skip ci]"
          git push origin HEAD:main
