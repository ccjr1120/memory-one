import React, { useEffect, useMemo, useState } from "react";
import { createRoot } from "react-dom/client";
import { Archive, ArrowUpRight, BookOpen, Brain, Check, ChevronDown, Clock3, Command, Copy, Database, FileText, Filter, Layers3, Menu, Moon, MoreHorizontal, Plus, Search, Server, Settings2, Sparkles, Sun, Tag, X } from "lucide-react";
import "./styles.css";

type Memory = { id: string; content: string; kind: string; scope: string; project?: string | null; source?: string | null; occurred_at?: string | null; created_at: string; updated_at: string; confidence: number; importance: number; metadata: Record<string, unknown> };
const kinds = ["all", "fact", "preference", "episode", "procedure", "message"];
type Language = "zh";
const kindLabels: Record<string, string> = { all: "全部", fact: "事实", preference: "偏好", episode: "事件", procedure: "方法", message: "对话" };
type View = "all" | "timeline" | "archive" | "preferences" | "projects" | "tags" | "settings" | "mcp";
const viewCopy: Record<View, { title: string; description: string }> = {
    all: { title: "全部记忆", description: "一个安静、可检索的空间，保存 Agent 需要记住的事实、偏好和经验。" },
    timeline: { title: "时间线", description: "按发生和写入时间回看你的记忆。" },
    archive: { title: "归档", description: "被归档的记忆会集中显示在这里。" },
    preferences: { title: "偏好与习惯", description: "集中查看 Agent 需要遵循的个人偏好和习惯。" },
    projects: { title: "Scope 分类", description: "按 scope 分类查看记忆，项目目录名只是其中一种常用值。" },
    tags: { title: "标签", description: "标签视图将在记忆拥有标签后显示内容。" },
    settings: { title: "设置", description: "本地存储和界面设置。" },
    mcp: { title: "MCP 服务", description: "把 Memory One 连接到你的 Agent，查看端点配置和可用工具。" },
};
const ui = {
  zh: { brandSubtitle: "个人记忆库", workspace: "工作区", collections: "集合", connect: "连接", navAll: "全部记忆", navTimeline: "时间线", navArchive: "归档", navPreferences: "偏好与习惯", navScopes: "Scope 分类", navTags: "标签", navMcp: "MCP 服务", navSettings: "设置", localStorage: "本地存储", memories: "MEMORIES", scopes: "SCOPES", scopesNote: "分类数量", lastSync: "LAST SYNC", justNow: "刚刚", localDatabase: "本地数据库", localOnly: "完全本地", localOnlyDesc: "你的记忆只留在这台设备", newMemory: "新建记忆", search: "搜索记忆...", allScopes: "所有 Scope", reading: "读取中...", countSuffix: "条记忆", sortRecent: "按最近更新排序", sortTimeline: "按发生时间排序", sortGrouped: "按 Scope 分组", globalMemory: "全局记忆", emptyMatch: "这里还没有匹配的记忆", emptyHelp: "试试换一个关键词，或创建一条新的记忆。", noArchive: "暂无归档记忆", noTags: "暂无标签", settingsManaged: "设置已由本地配置管理", detailEyebrow: "记忆详情", close: "关闭", detailEmpty: "选择一条记忆", detailEmptyDesc: "查看完整内容、来源和元数据", confidence: "置信度", created: "创建时间", updated: "最后更新", source: "来源", agentWritten: "Agent 写入", scope: "Scope", signals: "信号", importance: "重要性", original: "查看原始记录", switchLanguage: "Switch to English", newMemoryEyebrow: "新建记忆", composerTitle: "保存一条记忆", content: "记忆内容", contentPlaceholder: "例如：用户偏好使用简洁的工具界面。", type: "类型", scopeOptional: "可选，例如：/path/to/project", sourcePlaceholder: "例如：Claude Code / 手动添加", writeLocal: "写入本地 SQLite", cancel: "取消", save: "保存记忆", languageCode: "EN", mcpKicker: "模型上下文协议", mcpTitle: "连接你的 Agent", mcpDescription: "Memory One 通过 Streamable HTTP 提供记忆工具。把下面的配置加入 MCP 客户端，Agent 就能读写这台设备上的记忆。", mcpOnline: "HTTP 在线", mcpConfig: "通用 MCP 配置", mcpEndpoint: "服务地址", mcpCopyConfig: "复制配置", mcpCopyEndpoint: "复制服务地址", mcpClientNote: "适用于支持 HTTP MCP 的客户端，例如 Codex、Claude Code、Cursor 和自建 Agent。需要分类时，传入对应的 scope，例如项目目录名。", transport: "传输方式", auth: "认证", noAuth: "本机无需认证", category: "记忆分类", categoryValue: "全局 / 可选 scope", toolsLabel: "可用工具", toolsTitle: "Agent 可以使用的工具", toolsCount: "个工具", http: "HTTP", localMemory: "本地记忆 / 01", clientConfig: "客户端配置", serverEndpoint: "服务端点" },
} as const;
const formatDate = (value: string | null | undefined, language: Language) => value ? new Intl.DateTimeFormat(language === "zh" ? "zh-CN" : "en-US", { month: "short", day: "numeric", hour: "2-digit", minute: "2-digit" }).format(new Date(value)) : language === "zh" ? "未标记时间" : "No date";

function App() {
  const [memories, setMemories] = useState<Memory[]>([]); const [selected, setSelected] = useState<Memory | null>(null); const [query, setQuery] = useState(""); const [kind, setKind] = useState("all"); const [project, setProject] = useState("all"); const [view, setView] = useState<View>("all"); const language: Language = "zh"; const [dark, setDark] = useState(() => localStorage.getItem("memory-one-theme") === "dark"); const [showComposer, setShowComposer] = useState(false); const [mobileNav, setMobileNav] = useState(false); const [loading, setLoading] = useState(true);
  const load = async (search = query) => { setLoading(true); const endpoint = search.trim() ? `/api/search?query=${encodeURIComponent(search)}` : "/api/memories?limit=100"; const response = await fetch(endpoint); setMemories(await response.json()); setLoading(false); };
  const copy = ui[language]; const labels = kindLabels;
  useEffect(() => { load(""); }, []); useEffect(() => { document.documentElement.dataset.theme = dark ? "dark" : "light"; localStorage.setItem("memory-one-theme", dark ? "dark" : "light"); }, [dark]); useEffect(() => { document.documentElement.lang = "zh-CN"; }, []);
  useEffect(() => { const onKey = (event: KeyboardEvent) => { if ((event.metaKey || event.ctrlKey) && event.key.toLowerCase() === "k") { event.preventDefault(); document.querySelector<HTMLInputElement>("#memory-search")?.focus(); } }; window.addEventListener("keydown", onKey); return () => window.removeEventListener("keydown", onKey); }, []);
  const projects = useMemo(() => ["all", ...new Set(memories.map((item) => item.project).filter(Boolean) as string[])], [memories]);
  const filtered = memories.filter((item) => {
    if (view === "archive" || view === "tags" || view === "settings" || view === "mcp") return false;
    if (view === "preferences" && item.kind !== "preference") return false;
    if (view === "projects" && !item.project) return false;
    return (kind === "all" || item.kind === kind) && (project === "all" || item.project === project);
  });
  const memoryGroups = useMemo(() => {
    const groups = new Map<string, Memory[]>();
    for (const item of filtered) {
      const key = item.project ?? "__global__";
      groups.set(key, [...(groups.get(key) ?? []), item]);
    }
    return [...groups.entries()];
  }, [filtered]);
  const stats = { total: memories.length, projects: new Set(memories.map((item) => item.project).filter(Boolean)).size, preferences: memories.filter((item) => item.kind === "preference").length };
  const activeCopy = viewCopy[view];
  const selectView = (nextView: View) => { setView(nextView); setSelected(null); setMobileNav(false); setQuery(""); setKind(nextView === "preferences" ? "preference" : "all"); setProject("all"); void load(""); };
  const createMemory = async (payload: { content: string; kind: string; project: string; source: string }) => { const response = await fetch("/api/memories", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ ...payload, project: payload.project || null, source: payload.source || null }) }); const memory = await response.json(); setMemories((current) => [memory, ...current]); setSelected(memory); setShowComposer(false); };
  return <div className="app-shell">
    <aside className={`sidebar ${mobileNav ? "sidebar-open" : ""}`}><div className="brand"><div className="brand-mark"><Brain size={17} /></div><div><strong>Memory One</strong><span>{copy.brandSubtitle}</span></div><button className="icon-button mobile-close" onClick={() => setMobileNav(false)} title={copy.close}><X size={17} /></button></div><NavSection title={copy.workspace}><NavItem icon={<BookOpen size={17} />} label={copy.navAll} count={stats.total} active={view === "all"} onClick={() => selectView("all")} /><NavItem icon={<Clock3 size={17} />} label={copy.navTimeline} active={view === "timeline"} onClick={() => selectView("timeline")} /><NavItem icon={<Archive size={17} />} label={copy.navArchive} active={view === "archive"} onClick={() => selectView("archive")} /></NavSection><NavSection title={copy.collections}><NavItem icon={<Sparkles size={17} />} label={copy.navPreferences} count={stats.preferences} active={view === "preferences"} onClick={() => selectView("preferences")} /><NavItem icon={<Layers3 size={17} />} label={copy.navScopes} count={stats.projects} active={view === "projects"} onClick={() => selectView("projects")} /><NavItem icon={<Tag size={17} />} label={copy.navTags} active={view === "tags"} onClick={() => selectView("tags")} /></NavSection><NavSection title={copy.connect}><NavItem icon={<Server size={17} />} label={copy.navMcp} active={view === "mcp"} onClick={() => selectView("mcp")} /></NavSection><div className="sidebar-footer"><div className="storage-status"><span className="status-dot" />{copy.localStorage}<span className="mono">SQLite</span></div><NavItem icon={<Settings2 size={17} />} label={copy.navSettings} active={view === "settings"} onClick={() => selectView("settings")} /></div></aside>{mobileNav && <button className="sidebar-backdrop" onClick={() => setMobileNav(false)} aria-label={copy.close} />}
    <main className="main-content"><header className="topbar"><button className="icon-button menu-button" onClick={() => setMobileNav(true)} title={copy.close}><Menu size={19} /></button><div className="breadcrumbs"><span>{copy.workspace}</span><ChevronDown size={14} /><strong>{activeCopy.title}</strong></div><div className="top-actions"><button className="icon-button" onClick={() => setDark((value) => !value)} title={dark ? "切换浅色模式" : "切换深色模式"}>{dark ? <Sun size={17} /> : <Moon size={17} />}</button><button className="icon-button" title="更多操作"><MoreHorizontal size={18} /></button><div className="avatar">C</div></div></header><div className="content-wrap">{view === "mcp" ? <McpPage language={language} /> : <><section className="page-heading"><div><div className="heading-kicker"><span className="live-dot" />{copy.localMemory}</div><h1>{activeCopy.title}</h1><p>{activeCopy.description}</p></div><button className="primary-button" onClick={() => setShowComposer(true)}><Plus size={17} />{copy.newMemory}</button></section><section className="stat-grid"><Stat label={copy.memories} value={stats.total.toString().padStart(2, "0")} note="已保存" /><Stat label={copy.scopes} value={stats.projects.toString().padStart(2, "0")} note={copy.scopesNote} /><Stat label={copy.lastSync} value={copy.justNow} note={copy.localDatabase} date /><div className="stat-card stat-callout"><Database size={19} /><div><strong>{copy.localOnly}</strong><span>{copy.localOnlyDesc}</span></div></div></section><section className="toolbar"><div className="search-box"><Search size={17} /><input id="memory-search" value={query} onChange={(event) => setQuery(event.target.value)} onKeyDown={(event) => { if (event.key === "Enter") load(); }} placeholder={copy.search} /><kbd><Command size={11} />K</kbd></div><div className="toolbar-actions"><div className="select-wrap"><Filter size={15} /><select value={kind} onChange={(event) => setKind(event.target.value)}>{kinds.map((value) => <option key={value} value={value}>{labels[value]}</option>)}</select></div><div className="select-wrap project-select"><select value={project} onChange={(event) => setProject(event.target.value)}>{projects.map((value) => <option key={value} value={value}>{value === "all" ? copy.allScopes : value}</option>)}</select><ChevronDown size={14} /></div></div></section><div className="list-header"><span>{loading ? copy.reading : `${filtered.length}${copy.countSuffix}`}</span><span className="mono">{view === "timeline" ? copy.sortTimeline : view === "all" && project === "all" ? copy.sortGrouped : copy.sortRecent}</span></div><section className="memory-list">{!loading && filtered.length === 0 && <div className="empty-state"><FileText size={28} /><strong>{view === "archive" ? copy.noArchive : view === "tags" ? copy.noTags : view === "settings" ? copy.settingsManaged : copy.emptyMatch}</strong><span>{view === "archive" || view === "tags" || view === "settings" ? activeCopy.description : copy.emptyHelp}</span></div>}{view === "all" && project === "all" ? memoryGroups.map(([group, items]) => <div className="memory-group" key={group}><div className="memory-group-header"><span>{group === "__global__" ? copy.globalMemory : group}</span><span className="mono">{items.length.toString().padStart(2, "0")}</span></div>{items.map((item) => <MemoryCard key={item.id} item={item} selected={selected?.id === item.id} onClick={() => setSelected(item)} language={language} />)}</div>) : filtered.map((item) => <MemoryCard key={item.id} item={item} selected={selected?.id === item.id} onClick={() => setSelected(item)} language={language} />)}</section></>}</div></main>
    <aside className="detail-panel"><div className="detail-header"><span className="eyebrow">{copy.detailEyebrow}</span>{selected && <button className="icon-button" onClick={() => setSelected(null)} title={copy.close}><X size={17} /></button>}</div>{selected ? <MemoryDetail item={selected} language={language} /> : <div className="detail-empty"><div className="detail-icon"><ArrowUpRight size={20} /></div><strong>{copy.detailEmpty}</strong><span>{copy.detailEmptyDesc}</span></div>}</aside>{showComposer && <Composer onClose={() => setShowComposer(false)} onCreate={createMemory} language={language} />}
  </div>;
}
function NavSection({ title, children }: { title: string; children: React.ReactNode }) { return <div className="sidebar-section"><span className="eyebrow">{title}</span>{children}</div>; }
function NavItem({ icon, label, count, active, onClick }: { icon: React.ReactNode; label: string; count?: number; active?: boolean; onClick: () => void }) { return <button className={`nav-item ${active ? "active" : ""}`} onClick={onClick} aria-current={active ? "page" : undefined}>{icon}<span>{label}</span>{count !== undefined && <b>{count}</b>}</button>; }
const mcpTools = [
  ["memory_store", "Store durable memory with optional scope.", "保存一条持久记忆，可选 scope 分类。"],
  ["memory_search", "Search memories with SQLite FTS5 and an optional scope filter.", "使用 SQLite FTS5 搜索记忆，可按 scope 筛选。"],
  ["memory_get_context", "Return recent or query-focused context with an optional scope filter.", "获取近期或相关上下文，可按 scope 筛选。"],
  ["memory_get", "Get one memory by ID.", "按 ID 获取单条记忆。"],
  ["memory_list", "List memories with an optional scope filter.", "列出记忆，可按 scope 筛选。"],
  ["memory_update", "Update a memory or its metadata.", "更新记忆内容或元数据。"],
  ["memory_delete", "Soft-delete a memory.", "软删除一条记忆。"],
  ["memory_feedback", "Record whether a memory was useful.", "记录记忆是否有帮助的相关性信号。"],
] as const;
function McpPage({ language }: { language: Language }) {
  const copy = ui[language];
  const [copied, setCopied] = useState<string | null>(null);
  const host = typeof window !== "undefined" && window.location.hostname ? window.location.hostname : "127.0.0.1";
  const pagePort = typeof window !== "undefined" ? window.location.port : "";
  const serverPort = pagePort === "5173" || !pagePort ? "8765" : pagePort;
  const endpoint = `http://${host}:${serverPort}/mcp/`;
  const config = JSON.stringify({ mcpServers: { "memory-one": { type: "http", url: endpoint } } }, null, 2);
  const copyText = async (value: string, key: string) => { await navigator.clipboard?.writeText(value); setCopied(key); window.setTimeout(() => setCopied(null), 1500); };
  return <div className="mcp-page"><div className="mcp-hero"><div><div className="heading-kicker"><span className="live-dot" />{copy.mcpKicker}</div><h1>{copy.mcpTitle}</h1><p>{copy.mcpDescription}</p></div><div className="mcp-status"><span className="status-dot" /><strong>{copy.mcpOnline}</strong><span className="mono">{host}:{serverPort}</span></div></div><div className="mcp-grid"><section className="mcp-panel mcp-config"><div className="mcp-panel-header"><div><span className="eyebrow">{copy.clientConfig}</span><h2>{copy.mcpConfig}</h2></div><button className="icon-button" onClick={() => copyText(config, "config")} title={copy.mcpCopyConfig}>{copied === "config" ? <Check size={16} /> : <Copy size={16} />}</button></div><pre><code>{config}</code></pre><div className="mcp-note"><Server size={15} /><span>{copy.mcpClientNote}</span></div></section><section className="mcp-panel mcp-endpoint"><div className="mcp-panel-header"><div><span className="eyebrow">{copy.serverEndpoint}</span><h2>{copy.mcpEndpoint}</h2></div><button className="icon-button" onClick={() => copyText(endpoint, "endpoint")} title={copy.mcpCopyEndpoint}>{copied === "endpoint" ? <Check size={16} /> : <Copy size={16} />}</button></div><div className="endpoint-value">{endpoint}</div><div className="endpoint-meta"><span><i className="status-dot" />{copy.transport}</span><strong>Streamable HTTP</strong></div><div className="endpoint-meta"><span><i className="status-dot" />{copy.auth}</span><strong>{copy.noAuth}</strong></div><div className="endpoint-meta"><span><i className="status-dot" />{copy.category}</span><strong>{copy.categoryValue}</strong></div></section></div><section className="mcp-tools"><div className="mcp-tools-heading"><div><span className="eyebrow">{copy.toolsLabel}</span><h2>{copy.toolsTitle}</h2></div><span className="tool-count">{mcpTools.length} {copy.toolsCount}</span></div><div className="tool-list">{mcpTools.map(([name, enDescription, zhDescription]) => <article className="tool-row" key={name}><div className="tool-icon"><Server size={15} /></div><div><strong>{name}</strong><span>{language === "zh" ? zhDescription : enDescription}</span></div><code>{copy.http}</code></article>)}</div></section></div>;
}
function Stat({ label, value, note, date }: { label: string; value: string; note: string; date?: boolean }) { return <div className="stat-card"><span className="stat-label">{label}</span><strong className={date ? "stat-date" : ""}>{value}</strong><span className="stat-note">{note}</span></div>; }
function MemoryCard({ item, selected, onClick, language }: { item: Memory; selected: boolean; onClick: () => void; language: Language }) { const copy = ui[language]; const labels = kindLabels; return <button className={`memory-card ${selected ? "selected" : ""}`} onClick={onClick}><div className="card-top"><span className={`kind-badge kind-${item.kind}`}>{labels[item.kind] ?? item.kind}</span><span className="card-date">{formatDate(item.occurred_at ?? item.created_at, language)}</span></div><p>{item.content}</p><div className="card-bottom"><span>{item.project ? <><Layers3 size={13} />{item.project}</> : <><Tag size={13} />{item.scope}</>}</span><span className="confidence"><span style={{ width: `${Math.round(item.confidence * 100)}%` }} />{copy.confidence} {Math.round(item.confidence * 100)}%</span></div></button>; }
function MemoryDetail({ item, language }: { item: Memory; language: Language }) { const copy = ui[language]; const labels = kindLabels; return <div className="detail-body"><div className={`detail-kind kind-badge kind-${item.kind}`}>{labels[item.kind] ?? item.kind}</div><h2>{item.content}</h2><div className="detail-meta"><div><span>{copy.created}</span><strong>{formatDate(item.created_at, language)}</strong></div><div><span>{copy.updated}</span><strong>{formatDate(item.updated_at, language)}</strong></div><div><span>{copy.source}</span><strong>{item.source ?? copy.agentWritten}</strong></div></div><div className="detail-section"><span className="eyebrow">{copy.scope}</span><div className="scope-value"><Database size={15} />{item.scope}{item.project && <><span>/</span>{item.project}</>}</div></div><div className="detail-section"><span className="eyebrow">{copy.signals}</span><Signal label={copy.importance} value={item.importance} language={language} /><Signal label={copy.confidence} value={item.confidence} language={language} /></div><button className="ghost-button detail-action">{copy.original}<ArrowUpRight size={14} /></button></div>; }
function Signal({ label, value, language }: { label: string; value: number; language: Language }) { return <div className="signal-row"><span>{label}</span><div className="signal-track"><span style={{ width: `${Math.round(value * 100)}%` }} /></div><b>{Math.round(value * 100)}%</b></div>; }
function Composer({ onClose, onCreate, language }: { onClose: () => void; onCreate: (payload: { content: string; kind: string; project: string; source: string }) => void; language: Language }) { const copy = ui[language]; const labels = kindLabels; const [content, setContent] = useState(""); const [kind, setKind] = useState("fact"); const [project, setProject] = useState(""); const [source, setSource] = useState(""); return <div className="modal-backdrop" onMouseDown={(event) => { if (event.target === event.currentTarget) onClose(); }}><div className="composer"><div className="composer-header"><div><span className="eyebrow">{copy.newMemoryEyebrow}</span><h2>{copy.composerTitle}</h2></div><button className="icon-button" onClick={onClose} title={copy.close}><X size={18} /></button></div><label>{copy.content}<textarea autoFocus value={content} onChange={(event) => setContent(event.target.value)} placeholder={copy.contentPlaceholder} rows={5} /></label><div className="composer-grid"><label>{copy.type}<select value={kind} onChange={(event) => setKind(event.target.value)}>{kinds.slice(1).map((value) => <option key={value} value={value}>{labels[value]}</option>)}</select></label><label>{copy.scope}<input value={project} onChange={(event) => setProject(event.target.value)} placeholder={copy.scopeOptional} /></label></div><label>{copy.source}<input value={source} onChange={(event) => setSource(event.target.value)} placeholder={copy.sourcePlaceholder} /></label><div className="composer-footer"><span><Check size={14} />{copy.writeLocal}</span><div><button className="ghost-button" onClick={onClose}>{copy.cancel}</button><button className="primary-button" disabled={!content.trim()} onClick={() => onCreate({ content, kind, project, source })}>{copy.save} <ArrowUpRight size={15} /></button></div></div></div></div>; }
createRoot(document.getElementById("root")!).render(<React.StrictMode><App /></React.StrictMode>);
